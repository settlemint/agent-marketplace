{
  "mcpServers": {
    "context7": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@context7/mcp@latest"],
      "description": "Fetch up-to-date documentation for any library. ALWAYS use this before implementing features."
    },
    "octocode": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@anthropics/octocode-mcp@latest"],
      "description": "Search GitHub repositories for code patterns and implementations."
    },
    "codex": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@anthropics/codex-mcp@latest"],
      "description": "Deep code reasoning and analysis."
    }
  }
}
