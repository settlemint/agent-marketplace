[metadata]
name = "git-workflow"
description = "Git workflow commands for commits, branches, PRs, reviews, and post-PR automation (status, merge)"
icon = "git-branch"
color = "#F05032"

[defaults]
prompt = "commit"
