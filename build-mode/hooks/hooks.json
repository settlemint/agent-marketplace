{
  "description": "Advisory TDD guidance for Write/Edit operations - deterministic command hook",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\":{\"permissionDecision\":\"allow\"},\"systemMessage\":\"TDD REMINDER: Write a failing test FIRST, then implement, then refactor.\"}'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
